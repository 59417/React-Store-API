{
    "version": 2,
    "name": "react-store-api",
    "builds": [{
        "src": "/server.js",
        "use": "@now/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/server.js"
    }]
}